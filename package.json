{
  "name": "global-hotel-functions",
  "description": "Firebase Cloud Functions for Global Hotel/Restaurant/Travel Planner Platform",
  "version": "1.0.0",
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ext .ts src",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "test": "jest"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "stripe": "^15.0.0"
  },
  "devDependencies": {
    "@types/stripe": "^15.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^8.40.0",
    "typescript": "^5.2.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.5.0"
  },
  "private": true
}
